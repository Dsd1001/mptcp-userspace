# 0.9.0 delivery inventory

The generated distribution contains the Universal Mac DMG, Linux amd64 interactive Landing executable, exact source archive, source manifest, binary SHA256 sums, rebuild provenance and separate automatic/WAN acceptance records. The release package does not contain production transport keys or a modified user profile.

Use generated ACCEPTANCE.md for the actual gate status. The version number, successful build, or an existing 0.8.0 report is not proof that 0.9.0 passed capacity/performance/WAN acceptance. All current binary evidence must carry the new Source-ID.
